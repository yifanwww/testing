Install dotnet v8 or higher (need to change csproj file), run the following command and you will get the benchmark result:

```sh
> dotnet --version
> dotnet run
```

See log.txt for an example results.
